"""
Configuration file for Time Tracker Pro
"""

from pathlib import Path

# TEMPORARY: Use local database until we figure out Google Drive path
DB_PATH = Path("data/time_tracker.db")
DB_PATH.parent.mkdir(parents=True, exist_ok=True)

print(f"Using database at: {DB_PATH.absolute()}")
